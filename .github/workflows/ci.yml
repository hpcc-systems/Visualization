on:
  pull_request:
    branches:
      - main
      - candidate-*
  push:
    branches:
      - main
      - candidate-*

permissions:
  contents: write
  pull-requests: write

name: PR Testing

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Sources
        uses: actions/checkout@v6

      - name: Install NodeJS
        uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"
          scope: "@hpcc-js"

      - name: Install Dependencies
        run: |
          npm ci

      - name: Build
        run: |
          npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: build-output
          path: |
            packages/*/dist/
            packages/*/types/

      - name: Upload error logs
        if: ${{ failure() || cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: all-logs
          path: ./**/*.log
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Sources
        uses: actions/checkout@v6

      - name: Install NodeJS
        uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"
          scope: "@hpcc-js"

      - name: Install Dependencies
        run: |
          npm ci

      - name: Lint
        run: |
          npm run lint

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Sources
        uses: actions/checkout@v6

      - name: Install NodeJS
        uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"
          scope: "@hpcc-js"

      - name: Install Dependencies
        run: |
          npm ci

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: build-output
          path: packages

      - name: Install Test Dependencies
        run: |
          sudo apt-get update
          sudo npx -y playwright install chromium --with-deps
          npx -y playwright install chromium
          wget https://github.com/hpcc-systems/HPCC-Platform/releases/download/community_10.4.14-1/hpccsystems-platform-community_10.4.14-1noble_amd64_withsymbols.deb

      - name: Install HPCC Platform
        continue-on-error: true
        run: |
          sudo apt install -f ./hpccsystems-platform-community_10.4.14-1noble_amd64_withsymbols.deb
          sudo /etc/init.d/hpcc-init start

      - name: Test
        env:
          CI: true
        run: |
          npm run test
          npm run test-browser-esm
          npm run test-browser-umd
          npm run test-node-esm
          npm run test-node-cjs

      - name: Upload error logs
        if: ${{ failure() || cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: all-logs
          path: ./**/*.log
